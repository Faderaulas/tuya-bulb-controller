# Local configuration / secrets (NEVER commit — contains the bulb's local key)
dispositivos.json
preferencias.json
# legacy English names (kept ignored so an old local file is never committed)
devices.json
preferences.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# PyInstaller build output
build/
dist/
*.spec

# Misc
_icon_tmp.png
*.log
.DS_Store
Thumbs.db
